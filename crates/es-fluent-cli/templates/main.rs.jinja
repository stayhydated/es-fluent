// Auto-generated by es-fluent CLI
use ::{{ crate_ident }}::*;
use es_fluent::EsFluentGenerator;

fn main() {
    // Read config from parent crate's i18n.toml
    let config = es_fluent::es_fluent_toml::I18nConfig::read_from_path(
        "{{ i18n_toml_path }}"
    ).expect("Failed to read i18n.toml");

    let output_path = config.assets_dir.join(&config.fallback_language);

    EsFluentGenerator::builder()
        .output_path(output_path)
        .build()
        .generate()
        .expect("Failed to generate FTL files");
}
