{
  "name": "es-fluent",
  "private": true,
  "workspaces": [
    "web"
  ],
  "scripts": {
    "build": "turbo build",
    "build:wasm": "bun run build:wasm:bevy",
    "build:wasm:bevy": "cargo build --release --target wasm32-unknown-unknown -p bevy-example --no-default-features && ~/.cargo/bin/wasm-bindgen --out-dir web/public/bevy-example --target web target/wasm32-unknown-unknown/release/bevy-example.wasm && cp -r examples/assets web/public/bevy-example/",
    "build:web": "turbo run build --filter=web",
    "dev": "turbo dev",
    "fmt": "oxfmt",
    "lint": "oxlint"
  },
  "devDependencies": {
    "oxfmt": "^0.31.0",
    "oxlint": "^1.46.0",
    "turbo": "^2.8.7"
  },
  "packageManager": "bun@1.3.5"
}
