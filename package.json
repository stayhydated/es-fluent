{
  "name": "es-fluent",
  "private": true,
  "workspaces": [
    "web"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "build:wasm": "cargo build --release --target wasm32-unknown-unknown -p bevy-example --no-default-features && ~/.cargo/bin/wasm-bindgen --out-dir web/public/bevy-example --target web target/wasm32-unknown-unknown/release/bevy-example.wasm && cp -r examples/assets web/public/bevy-example/",
    "build:web": "turbo run build --filter=web"
  },
  "devDependencies": {
    "turbo": "^2"
  },
  "packageManager": "bun@1.3.5"
}
