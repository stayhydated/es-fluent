# [unstable]
# codegen-backend = true
# [profile.server-dev]
# codegen-backend = "cranelift"

[alias]
xtask = "run -p xtask --"
es-fluent-local = "run -p es-fluent-cli -- es-fluent"

[target.x86_64-unknown-linux-gnu]
linker = "clang"
rustflags = [ "-C", "link-arg=-fuse-ld=mold" ]
