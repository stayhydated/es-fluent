# [unstable]
# codegen-backend = true
# [profile.server-dev]
# codegen-backend = "cranelift"

[alias]
generate-lang-names = "run -p xtask -- generate-lang-names"

[target.x86_64-unknown-linux-gnu]
linker = "clang"
rustflags = [ "-C", "link-arg=-fuse-ld=mold" ]
