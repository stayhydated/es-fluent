# web

The `web` package builds the es-fluent project site and its Ember & Echo Dioxus
demo. The `/dioxus-example/` route demonstrates reactive localization with the
Dioxus manager across `en-US`, `fr-FR`, and `zh-CN`.
