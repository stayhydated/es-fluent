## DemoCopy

demo_copy-Kicker = Framework-neutral TypeScript
demo_copy-Title = One Rust contract, one browser runtime.
demo_copy-Body = The generated descriptors stay typed while Project Fluent formats this page with no Rust code at runtime.
demo_copy-SwitchLocale = Switch language

## LocaleStatus

locale_status = Active locale: { $locale }

## Greeting

greeting = Welcome back, { $name }.

## Inbox

inbox =
    { $count ->
        [one] You have one localized message.
       *[other] You have { $count } localized messages.
    }
