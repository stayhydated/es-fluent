[package]
name = "example-shared-lib"
version.workspace = true
edition.workspace = true
license = "Unlicense"
publish = false

[dependencies]
bevy = { optional = true, workspace = true }
es-fluent = { features = [ "derive" ], workspace = true }
es-fluent-lang = { workspace = true }
es-fluent-manager-bevy = { optional = true, workspace = true }
es-fluent-manager-embedded = { optional = true, workspace = true }
gpui = { optional = true, workspace = true }
rust-embed = { optional = true, workspace = true }
strum = { features = [ "derive" ], workspace = true }
unic-langid = { workspace = true }

[build-dependencies]
es-fluent-build = { workspace = true }
log = { workspace = true }

[lints]
workspace = true

[features]
bevy = [ "dep:bevy", "dep:es-fluent-manager-bevy" ]
gpui = [ "dep:gpui", "dep:rust-embed", "dep:es-fluent-manager-embedded" ]
iced = [ "dep:rust-embed", "dep:es-fluent-manager-embedded" ]
