// Generated by `cargo es-fluent export typescript`.
import type { ResourceLoader } from "@es-fluent/core";

export const resourceSources = {
  "locales/en/expo-example/expo-example.ftl": "## DemoCopy\n\ndemo_copy-Body = iOS and Android format in Rust through UniFFI. Expo web uses the same descriptors and React facade with the TypeScript runtime.\ndemo_copy-Kicker = @es-fluent/expo\ndemo_copy-SwitchLocale = Switch language\ndemo_copy-Title = One contract, two runtimes.\n\n## Greeting\n\ngreeting = Hello from the Expo runtime, { $name }.\n\n## Inbox\n\ninbox =\n    { $count ->\n        [one] One localized message is ready.\n       *[other] { $count } localized messages are ready.\n    }\n\n## LocaleStatus\n\nlocale_status = Active locale: { $locale }\n",
  "locales/fr/expo-example/expo-example.ftl": "## DemoCopy\n\ndemo_copy-Body = iOS et Android utilisent Rust via UniFFI. Expo web partage les mêmes descripteurs et la façade React avec le moteur TypeScript.\n# es-fluent: same-as-fallback\ndemo_copy-Kicker = @es-fluent/expo\ndemo_copy-SwitchLocale = Changer de langue\ndemo_copy-Title = Un contrat, deux moteurs.\n\n## Greeting\n\ngreeting = Bonjour depuis le moteur Expo, { $name }.\n\n## Inbox\n\ninbox =\n    { $count ->\n        [one] Un message localisé est prêt.\n       *[other] { $count } messages localisés sont prêts.\n    }\n\n## LocaleStatus\n\nlocale_status = Langue active : { $locale }\n",
} as const;

export const loadResource: ResourceLoader = (resource) => {
const sources: Readonly<Record<string, string>> = resourceSources;
const source = sources[resource.path];
if (source === undefined) {
throw new Error(`Missing exported Fluent resource: ${resource.path}`);
}
return source;
};
