# React web example

This example exports a Rust-owned message contract and uses it through the
React 19 external-store facade from `@es-fluent/react`.

```sh
npm run export:i18n --workspace @es-fluent/example-react
npm run dev --workspace @es-fluent/example-react
```

The hosted build is available from the site's **TypeScript demos** page.
